import { Megrim, Nunito } from "next/font/google";

export const megrim = Megrim({
  variable: "--font-megrim",
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});

export const nunito = Nunito({
  variable: "--font-delius",
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});
